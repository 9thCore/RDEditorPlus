namespace RDEditorPlus
{
    internal static class PatchHandler
    {
        // This exists JUST so that renaming the namespace of a real PatchHandler doesn't cause VS to remove its namespace from Plugin.cs lol
    }
}
